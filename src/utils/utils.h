#ifndef LED_CONTROLLER_UTILS_H
#define LED_CONTROLLER_UTILS_H

#include <Arduino.h>
#include <Lamp.h>

// #include "settings.h"

class Utils {
public:

  // static void  getAllTopics(String action,
  //                           String topics[]);
  // static Lamp* getLampFromTopic(String topic);
  // static Lamp* getLampWithInputSwitchPin(uint8_t inputSwitchPin);
};

#endif // ifndef LED_CONTROLLER_UTILS_H
